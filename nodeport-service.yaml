# nodeport-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: cicd-test-service
  namespace: backend
spec:
  type: NodePort
  selector:
    app: cicd-test
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080  # 30000-32767 범위